<?xml version="1.0" encoding="utf-8"?>

<odoo>

    <!-- Hide first option 'Invoices' from Print -->
    <report
        id="account.account_invoices"
        model="account.invoice"
        string="Invoices"
        report_type="qweb-pdf"
        name="account.report_invoice_with_payments"
        file="account.report_invoice_with_payments"
        attachment="(object.state in ('open','in_payment','paid')) and ('INV'+(object.number or '').replace('/','')+'.pdf')"
        print_report_name="(object._get_report_base_filename())"
        groups="account.group_account_invoice"
        menu="False"
    />

    <!-- Second option as 'Rental Invoice' from Print -->
    <report
        id="account.account_invoices_without_payment"
        model="account.invoice"
        string="Rental Invoice"
        report_type="qweb-pdf"
        name="rental_invoice_report.report_invoice"
        file="rental_invoice_report.report_invoice"
        attachment="(object.state in ('open','in_payment','paid')) and ('INV'+(object.number or '').replace('/','')+'.pdf')"
        print_report_name="(object._get_report_base_filename())"
        groups="account.group_account_invoice"
    />

</odoo>
